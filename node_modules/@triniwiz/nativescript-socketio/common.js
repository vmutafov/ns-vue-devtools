/**
 *
 */
export class Common {
    get disconnected() {
        return !this.connected;
    }
    get instance() {
        return this.socket;
    }
    connect() {
        this.socket.connect();
    }
    disconnect() {
        this.socket.disconnect();
    }
}
//# sourceMappingURL=common.js.map
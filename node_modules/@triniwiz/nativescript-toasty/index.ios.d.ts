import { Color, Length } from '@nativescript/core';
import { ToastDuration, ToastPosition, ToastyOptions } from './common';
export { ToastDuration, ToastPosition, ToastyOptions } from './common';
export declare class Toasty {
    private _text;
    private _duration;
    private _position;
    private _textColor;
    private _backgroundColor;
    private _iOSOpts;
    private _toastStyle;
    private _x;
    private _y;
    private _tapToDismiss;
    private _anchorView;
    constructor(opts?: ToastyOptions);
    get position(): ToastPosition | number;
    set position(value: ToastPosition | number);
    get duration(): ToastDuration | number;
    set duration(value: ToastDuration | number);
    get textColor(): Color | string;
    set textColor(value: Color | string);
    get backgroundColor(): Color | string;
    set backgroundColor(value: Color | string);
    set xAxisOffset(value: Length | number);
    set yAxisOffset(value: Length | number);
    get xAxisOffset(): Length | number;
    get yAxisOffset(): Length | number;
    private static isLength;
    private get yPixels();
    private get xPixels();
    private getOffset;
    show(): void;
    get width(): number;
    get height(): number;
    cancel(): void;
    setTextColor(value: Color | string): this;
    setBackgroundColor(value: Color | string): this;
    private _updateToastPosition;
    setToastPosition(value: ToastPosition | number): this;
    setToastDuration(value: ToastDuration | number): this;
    private _getView;
    private static get rootViewController();
    private get topViewController();
    private findTopViewController;
}
